const KEY='bjfu-wifi-settings';const form=document.querySelector('#loginForm');const account=document.querySelector('#account');const password=document.querySelector('#password');const remember=document.querySelector('#rememberPassword');const auto=document.querySelector('#autoReconnect');const status=document.querySelector('#status');
function read(){try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return {}}}function save(){const s={account:account.value.trim(),auto:auto.checked};if(remember.checked&&password.value){s.password=password.value;s.remember=true}else{s.remember=false}localStorage.setItem(KEY,JSON.stringify(s))}function load(){const s=read();if(s.account)account.value=s.account;if(s.remember&&s.password){password.value=s.password;remember.checked=true}auto.checked=!!s.auto}
form.addEventListener('submit',(event)=>{event.preventDefault();if(!form.reportValidity())return;save();document.querySelector('#nonce').value=String(Date.now());const query=new URLSearchParams(new FormData(form));status.textContent='正在提交认证…';window.location.assign(`${form.action}?${query.toString()}`);});remember.addEventListener('change',()=>{if(remember.checked&&!confirm('记住密码会将密码保存在本机浏览器中。此方式不等同于系统钥匙串，确定开启吗？'))remember.checked=false});document.querySelector('#clear').onclick=()=>{localStorage.removeItem(KEY);form.reset();status.textContent='本地凭据已清除';};document.querySelector('#reconfigure').onclick=()=>{password.value='';account.focus();status.textContent='请输入新的账号和密码';};load();if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{});if(auto.checked&&account.value&&password.value){setTimeout(()=>form.requestSubmit(),500)}
